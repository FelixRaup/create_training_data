[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "PychemToolkit"
version = "0.1.0"
description = "Toolkit for creating training data with Pychem"

[tool.setuptools.packages.find]
where = ["."]